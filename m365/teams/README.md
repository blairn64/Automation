# Microsoft Teams operations lab

Inventory Teams using synthetic fixtures or Microsoft Graph in an authorised tenant.

```powershell
./Get-TeamsInventory.ps1 -Mode Fixture
```

The live path uses the Microsoft Graph Teams module. The repository stores no tenant content.